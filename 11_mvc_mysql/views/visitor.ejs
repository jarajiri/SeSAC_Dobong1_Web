<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <!-- axios -->
    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
    <!-- custom -->
    <script defer src="/static/visitor.js"></script>
  </head>
  <body>
    <h2>방명록 페이지</h2>
    <form name="visitor-form">
      <fieldset>
        <legend>방명록 등록</legend>
        <input type="text" name="name" placeholder="사용자이름" /><br />
        <input type="text" name="comment" placeholder="방명록 작성" /><br />
        <div id="btn-group">
          <button type="button" onclick="createVisitor()">방명록 등록</button>
        </div>
      </fieldset>
      <br />
      <br />
      <table border="1" cellspacing="0">
        <thead>
          <tr>
            <th>ID</th>
            <th>작성자</th>
            <th>방명록</th>
            <th>수정</th>
            <th>삭제</th>
          </tr>
        </thead>
        <tbody>
          <!-- <tr>
            <td>1</td>
            <td>name</td>
            <td>comment</td>
            <td>
              <button type="button">수정</button>
            </td>
            <td>
              <button type="button">삭제</button>
            </td>
          </tr> -->
          <!-- for문 작성 -->
          <% for(user of userInfo) {%>
          <tr id="tr_<%= user.id %>">
            <td><%= user.id %></td>
            <td><%= user.name %></td>
            <td><%= user.comment %></td>
            <td>
              <button type="button" onclick="editVisitor('<%= user.id %>')">수정</button>
            </td>
            <td>
              <button type="button" onclick="deleteVisitor(this,'<%= user.id %>')">삭제</button>
            </td>
          </tr>
          <%}%>
        </tbody>
      </table>
    </form>
  </body>
</html>
