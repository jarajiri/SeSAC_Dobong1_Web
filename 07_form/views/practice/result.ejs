<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>result</title>
    <style>
      .name {
        color: coral;
        font-weight: 700;
      }
      ul {
        list-style: none;
        padding: 0;
        margin: 0;
      }
      .info {
        border: 1px solid #bbb;
        box-shadow: 0 0 5px #c8c8c8;
        padding: 0.5rem;
        margin: 0.5rem 0;
        border-radius: 10px;
      }
    </style>
  </head>
  <body>
    <h2><%=title %>요청 확인하기</h2>
    <div class="name">이름 : <%= userInfo.name %></div>
    <div class="info">성별 : <%= userInfo.gender %></div>
    <div class="info">
      생년월일 : <%= userInfo.birthYear %>년 <%= userInfo.birthMonth %>월 <%= userInfo.birthDay %>일
    </div>
    <div class="info">관심사 :<%= userInfo.like %></div>

    <% if(addInfo) {%>
    <div class="info">좋아하는 색깔 : <span class="color"><%= userInfo.color %></span></div>
    <div class="info">좋아하는 숫자 :<%= userInfo.number %></div>
    <div class="info">코멘트 :<%= userInfo.comment %></div>
    <% } %>
  </body>
  <script>
    const color = document.querySelector(".color");
    document.addEventListener("DOMContentLoaded", () => {
      color.style.color = "<%= userInfo.color%>";
    });
    //
  </script>
</html>
